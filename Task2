class Pair:

    numbers=[10,20,30,40,10,50,60,70,10,80,90,100]
    target=50
    pairs={}
    count=1
    for i in range(12):
       for j in range(12):
          if ( (numbers[i] + numbers[j]) == target ):
              pairs[count]={i:j}
              count=count+1
    print(pairs)          

